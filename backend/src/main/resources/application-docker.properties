spring.datasource.url=jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:beauty_clinic}
spring.datasource.username=${DB_USER:beauty_clinic}
spring.datasource.password=${DB_PASSWORD:beauty_clinic}

spring.data.redis.host=${REDIS_HOST:redis}
spring.data.redis.port=${REDIS_PORT:6379}
